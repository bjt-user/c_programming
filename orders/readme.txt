This program should create and maintain a database of orders.

Wanted features:
Security check if user tries to overwrite data. ("Do you really want to overwrite the xyz attribute of record "yxy...")
